interface Student {
  name: string;
  birth: string;
  attendance: string;
  // address: string;
  // class: string;
  // createdAt: Date;
  // gender: string;
  // phone: string;
  // teacher: string;
}

export default Student;
