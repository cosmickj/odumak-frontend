export interface Student {
  name: string;
  birth: string;
  attendance: string;
  // address: string;
  // class: string;
  // createdAt: Date;
  // gender: string;
  // phone: string;
  // teacher: string;
}

export interface StudentAttendance {
  name: string;
  birth: string;
  attendance: string;
}

export interface UserInfo {
  name: string;
  email: string;
  createdAt: Date;
}
